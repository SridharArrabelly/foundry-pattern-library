
## Modernize-all-patterns to new Foundry SDK — DONE 2026-07-10
- Audited every pattern: only Pattern 6 used legacy SDK. Patterns 1 (chat.completions),
  3/9 (MCP), 5 (Agent Framework) are intentional, not "old SDK".
- Pattern 6 enable_tracing.py: replaced AIAgentsInstrumentor with Agent Framework
  observability.enable_instrumentation(enable_sensitive_data=True). Verified.
- Pattern 7 run_eval.py: keyless (omit api_key -> Entra), + patch_openai_for_gpt5_judges()
  shim (max_tokens -> max_completion_tokens for gpt-5.4-mini judges). Verified scorecard.
- Pinned AZURE_TENANT_ID=00000000-... in .env + .env.example to stop DefaultAzureCredential
  grabbing a wrong-tenant token (was intermittently failing eval workers). Re-ran eval:
  5/5 lines completed, 0 failed, no tenant mismatch, GATE FAILED (groundedness 3.40<4.0 =
  the intended "caught before shipping" outcome). Deterministic now.
- No Python code imports azure-ai-agents anymore (azure.yaml ref is the azd EXTENSION, not
  the pkg). Left the dep pinned to avoid re-lock risk before the demo.
- README: added AZURE_TENANT_ID multi-tenant tip to Setup. Pattern 2/6/7 sections already
  reflect the new SDK + keyless. Diagrams unchanged (still accurate).

## Readme audit + hosted-agent "hang" — DONE 2026-07-10
- Diagnosed the "30-min hang": main.py runs ResponsesHostServer().run() = a blocking web
  server on :8088. It idles forever until a request arrives; user never POSTed one. The red
  169.254.169.254 traceback is a benign OTel Azure resource-detector IMDS probe (fast-fails
  on a laptop). Reproduced locally: started server, POSTed /responses -> correct C-1290
  answer in seconds. NOT a bug.
- hosted/README.md: rewrote "Run it locally" to make clear main.py BLOCKS (server), need a
  2nd terminal; added no-azd PowerShell Invoke-RestMethod one-liner + benign-traceback note.
- hosted .env/.env.example: added AZURE_TENANT_ID pin.
- 02 TALK-TRACK: added "blocks - it's a server, POST from 2nd terminal" hint to Demo B.
- Audited all docs: 06/07 TALK-TRACKs generic+correct; litellm mentions are intended
  narrative (customer's gateway); asst_ line is deliberate contrast. No stale refs.
- Deck: rewrote dia2 (Pattern 2 slide) to the two hosting models layout (A prompt-based /
  B hosted -> shared Entra Agent ID) + updated subtitle/bullets. Rebuilt foundry-patterns.pptx
  (13 slides). Verified slide 4 shapes in-bounds, no overlap.

## Hosted agent DEPLOYED to Foundry — DONE 2026-07-10
- Upgraded azd 1.25.6 -> 1.27.1; upgraded azure.ai.agents ext to 1.0.0-beta.5; installed
  microsoft.foundry ext (+deps). azd auth login --tenant-id 00000000 (MCAPS) as
  sarrabelly@microsoft.com (has Foundry Project Manager/Owner on your-project).
- Bound to EXISTING project via: azd ai agent init --no-prompt --project-id <your-project>
  --deploy-mode code --runtime python_3_13 --entry-point main.py --src ./src/rm-assistant
  --agent-name rm-assistant-hosted --force. (code deploy = ZIP remote build, no Docker.)
  Note: code-deploy runtimes are python_3_13/3_14 only (not 3_12).
- Made main.py robust: reads FOUNDRY_PROJECT_ENDPOINT (auto-injected) + model name from
  AZURE_AI_MODEL_DEPLOYMENT_NAME/MODEL_DEPLOYMENT_NAME with gpt-5.4-mini fallback. Added
  env: map back to azure.yaml. Added azure-identity to requirements.txt.
- azd deploy SUCCESS in 2m19s -> rm-assistant-hosted:1 active, own Entra Agent ID
  (Instance Identity Principal 6ee34cbe...). azd ai agent invoke -> correct C-1290 verdict.
  Playground URL captured (portal Agents > rm-assistant-hosted).
- azure.yaml regenerated by init (bound by endpoint, no new infra). .azure/ env created.
- Updated hosted/README.md (prereqs: azd>=1.27, ext, --tenant-id gotcha; deploy: exact
  init/deploy/show/invoke/monitor + runtime note) and 02 TALK-TRACK Demo B (local + portal).
- For demo: show local run (server + 2nd terminal) AND the deployed agent in the portal.
